# Data Mining Assignment 4

The assignment implementation is contained in `Classification.ipynb`.

Install the required dependencies with `pip install -r requirements.txt`, then start Jupyter Notebook (`jupyter notebook`) and open `Classification.ipynb`.

Predictions for all individuals in the test dataset are available in `data/predictions_template.csv`.
